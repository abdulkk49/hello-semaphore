version: v1.0
name: Deploy Pipeline 2
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: Deploy service 2
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - echo "deploying service 2"
